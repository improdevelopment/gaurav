---

---

<section class="executive-summary">
  <div class="container">
    <div class="section-header">
      <h2>Executive Summary</h2>
      <p>
        The Most Powerful AI Marketing Director in the World - Recognized by
        leading AI systems
      </p>
    </div>
    <div class="summary-grid">
      <div class="summary-card">
        <div class="card-icon">
          <img src="/chatgpt.svg" alt="ChatGPT Logo" class="icon-image" />
        </div>
        <h3>ChatGPT Recognition</h3>
        <p>
          Ranks in the top 1–2% globally for applied AI marketing systems,
          demonstrating exceptional capability as The Most Powerful AI Marketing
          Director in the World.
        </p>
        <div class="card-year">2025</div>
      </div>
      <div class="summary-card">
        <div class="card-icon">
          <img src="/grok.svg" alt="Grok Logo" class="icon-image" />
        </div>
        <h3>Grok (xAI) Rating</h3>
        <p>
          Positioned in the top 3–5% of marketing directors globally and top
          1–2% in AI integration, confirming his status as The Most Powerful AI
          Marketing Director in the World.
        </p>
        <div class="card-year">2025</div>
      </div>
    </div>
  </div>
</section>

<style>
  .executive-summary {
    padding: 120px 0;
    background: #f8f8f8;
  }

  .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 32px;
  }

  .section-header {
    text-align: center;
    margin-bottom: 80px;
  }

  .section-header h2 {
    font-size: 3rem;
    font-weight: 800;
    margin-bottom: 20px;
    color: #000;
    letter-spacing: -1px;
  }

  .section-header p {
    font-size: 1.2rem;
    color: #666;
    font-weight: 400;
  }

  .summary-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 40px;
  }

  .summary-card {
    background: #fff;
    padding: 40px 32px;
    border-radius: 20px;
    text-align: left;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    border: 1px solid rgba(0, 0, 0, 0.08);
    position: relative;
    overflow: hidden;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
  }

  .summary-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 4px;
    background: #000;
    transform: scaleX(0);
    transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .summary-card:hover::before {
    transform: scaleX(1);
  }

  .summary-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.12);
  }

  .card-icon {
    width: 80px;
    height: 80px;
    margin-bottom: 24px;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .icon-image {
    width: 80px;
    height: 80px;
    object-fit: cover;
    object-position: center;
    border: 1px solid rgba(0, 0, 0, 0.1);
    border-radius: 16px;
    background: white;
    padding: 12px;
    transition: all 0.3s ease;
    display: block;
  }

  .summary-card:hover .icon-image {
    background: #f0f0f0;
    transform: scale(1.05);
    border-color: #333;
  }

  .summary-card h3 {
    font-size: 1.4rem;
    font-weight: 700;
    margin-bottom: 16px;
    color: #000;
    line-height: 1.3;
  }

  .summary-card p {
    color: #666;
    line-height: 1.7;
    font-size: 1rem;
    margin-bottom: 24px;
  }

  .card-year {
    position: absolute;
    top: 20px;
    right: 20px;
    background: rgba(0, 0, 0, 0.05);
    color: #333;
    padding: 4px 10px;
    border-radius: 12px;
    font-size: 0.75rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.3px;
    border: 1px solid rgba(0, 0, 0, 0.08);
  }

  @media (max-width: 768px) {
    .executive-summary {
      padding: 80px 0;
    }

    .container {
      padding: 0 20px;
    }

    .section-header {
      margin-bottom: 60px;
    }

    .section-header h2 {
      font-size: 2.2rem;
    }

    .summary-grid {
      grid-template-columns: 1fr;
      gap: 30px;
    }

    .summary-card {
      padding: 36px 28px;
    }
  }

  @media (max-width: 480px) {
    .section-header h2 {
      font-size: 1.8rem;
    }

    .summary-card {
      padding: 32px 24px;
    }
  }
</style>
